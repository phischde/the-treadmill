{% set pageTitle = title or site.title %}
{% set documentTitle = site.title + ' · ' + pageTitle  %}
{% set pageDescription = description or site.description %}
{% set canonicalUrl = site.url ~ (page.url or '/') %}
{% set pageLanguage = language or site.language %}
{% set pageLocale = locale or site.locale %}
{% set pagePublishedAt = publishedAt or site.publishedAt %}
{% set pageModifiedAt = modifiedAt or site.modifiedAt %}
<title>{{ documentTitle }}</title>
<meta name="description" content="{{ pageDescription }}">
{% if page.url == '/impressum/' or page.url == '/privacy/' %}<meta name="robots" content="noindex, nofollow">{% endif %}
<link rel="canonical" href="{{ canonicalUrl }}">
<meta property="og:site_name" content="{{ site.title }}">
<meta property="og:title" content="{{ documentTitle }}">
<meta property="og:description" content="{{ pageDescription }}">
<meta property="og:type" content="{% if layout == 'story.njk' %}article{% else %}website{% endif %}">
<meta property="og:url" content="{{ canonicalUrl }}">
{% if site.socialImage %}
  <meta property="og:image" content="{{ site.url ~ site.socialImage }}">
{% endif %}
<meta property="og:locale" content="{{ pageLocale }}">
{% if layout == 'story.njk' %}
<meta name="author" content="{{ site.author.name }}">
<meta property="article:author" content="{{ site.author.url }}">
<meta property="article:published_time" content="{{ pagePublishedAt }}">
<meta property="article:modified_time" content="{{ pageModifiedAt }}">
{% endif %}
{% if site.socialImage %}
  <meta name="twitter:card" content="summary_large_image">
{% else %}
  <meta name="twitter:card" content="summary">
{% endif %}
<meta name="twitter:title" content="{{ documentTitle }}">
<meta name="twitter:description" content="{{ pageDescription }}">
{% if site.socialImage %}
  <meta name="twitter:image" content="{{ site.url ~ site.socialImage }}">
{% endif %}
<link rel="icon" href="/favicon.ico">
<link rel="apple-touch-icon" href="/apple-touch-icon.png" sizes="180x180">
<link rel="manifest" href="/site.webmanifest">
{% if layout == 'story.njk' %}
{% set schema = {
  "@context": "https://schema.org",
  "@type": "CreativeWork",
  "name": documentTitle,
  "author": {
    "@type": "Person",
    "name": site.author.name,
    "url": site.author.url
  },
  "description": pageDescription,
  "inLanguage": pageLanguage,
  "license": "https://creativecommons.org/licenses/by-nc-sa/4.0/",
  "url": canonicalUrl,
  "datePublished": pagePublishedAt,
  "dateModified": pageModifiedAt
} %}
{% else %}
{% set schema = {
  "@context": "https://schema.org",
  "@type": "CreativeWork",
  "name": documentTitle,
  "author": {
    "@type": "Person",
    "name": site.author.name,
    "url": site.author.url
  },
  "description": pageDescription,
  "inLanguage": pageLanguage,
  "license": "https://creativecommons.org/licenses/by-nc-sa/4.0/",
  "url": canonicalUrl
} %}
{% endif %}
<script type="application/ld+json">{{ schema | json | safe }}</script>
